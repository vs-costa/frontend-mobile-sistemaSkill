import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  addButton: {
    marginTop: 20,
    backgroundColor: '#0958d9',
  },
  logoutButton: {
    marginTop: 10,
    alignSelf: 'center',
  },
});